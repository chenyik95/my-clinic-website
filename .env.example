# Copy this file to .env.local and fill in your values
# .env.local is gitignored (see .gitignore)

# Optional: Use Google Places API instead of the built-in review scraper.
# Reviews work automatically from your Google Maps link — no API key required.
# GOOGLE_PLACES_API_KEY=
# GOOGLE_PLACE_ID=ChIJxxxxxxxxxxxx